services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - INTERNAL_API_KEY=${INTERNAL_API_KEY}
    volumes:
      - ./backend/data:/app/data
    